sudo: required

language: rust
rust:
    - nightly

cache: cargo

env:
    - KEYBASE_USER=passbase_test

install:
    - cargo build

before_script:
    - mv target/debug/passbase test/bin

script:
    - ./test/tests.sh
